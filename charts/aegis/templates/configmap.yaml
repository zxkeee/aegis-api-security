apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aegis.fullname" . }}-config
  labels:
    {{- include "aegis.labels" . | nindent 4 }}
data:
  gateway.yaml: |
    {{- .Values.gateway | toYaml | nindent 4 }}
